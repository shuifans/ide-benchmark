# raw run artifacts stay local (top-level runs/ only; results/runs/ 入仓)
/runs/
__pycache__/
*.pyc
.DS_Store

# real judge credentials
config/judge.json

# read-only reference clones
agent-coding-bench/
ai-ide-benchmark/
