<!-- prompt_version: v1 -->
# 任务：修复分页工具模块

当前目录下有一个 Python 模块 `pagination.py`，提供分页相关的工具函数。

该模块存在若干边界错误，导致部分分页结果不正确（例如页数计算和分页切片在特定输入下返回错误结果）。

要求：

1. 定位并修复 `pagination.py` 中的所有错误，使分页行为完全正确；
2. 不要修改模块的公开函数签名与返回值结构；
3. 修复完成后，请自行验证你的修改是否正确（方式自定）。
